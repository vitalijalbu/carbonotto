import React, { useRef } from "react";
import Link from "next/link";
;

const Header = () => {
  const ref = React.useRef();

  return (
    <div id="Nav">
header
    </div>
  );
};

export default Header;
